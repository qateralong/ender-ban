<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.ufhs</groupId>
  <artifactId>EnderBan</artifactId>
  <version>2.0</version>
  <name>EnderBan</name>
  <description>Plugin for disabling items in the Ender Chest (1.20 - 1.21.x, all Bukkit-based cores)</description>

  <properties>
    <!--
      1.20.1 requires Java 17 at minimum; every newer server (up to and
      including 1.21.9) runs on Java 21, which happily executes bytecode
      compiled for release 17. Compiling for 17 instead of 21 is what lets
      this jar load on the oldest supported version (1.20) as well as the
      newest.
    -->
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <repositories>
    <repository>
      <id>papermc-repo</id>
      <url>https://repo.papermc.io/repository/maven-public/</url>
    </repository>
  </repositories>

  <dependencies>
    <!--
      Deliberately pinned to the OLDEST supported Paper API (1.20.1) rather
      than the newest. Bukkit's public API only grows across versions, so
      code compiled against 1.20.1 keeps working unmodified on every later
      1.20.x/1.21.x release. Pinning to the newest API instead would risk
      calling methods that don't exist yet on older servers.
    -->
    <dependency>
      <groupId>io.papermc.paper</groupId>
      <artifactId>paper-api</artifactId>
      <version>1.20.1-R0.1-SNAPSHOT</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>EnderBan-${project.version}</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.11.0</version>
        <configuration>
          <release>${maven.compiler.release}</release>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
